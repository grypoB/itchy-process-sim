/** @file main author : Guillaume Duc **/

#ifndef NUMERICLIMIT_H_
#define NUMERICLIMIT_H_

#include <cfloat>

namespace NumericLimit {
	const double DOUBLE_MAX(DBL_MAX);
	const double DOUBLE_MIN(-DBL_MAX);
}
#endif
